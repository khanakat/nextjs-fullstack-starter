# =================================================================
# LOCAL CONFIGURATION - FULLSTACK TEMPLATE
# =================================================================
# Copy this file as .env.local and configure your values

# =================================================================
# DATABASE CONFIGURATION (CHOOSE ONE OPTION)
# =================================================================

# OPTION 1: Local PostgreSQL (Recommended for local development)
# Install with: npm run postgres:local:install
# Start with:   npm run postgres:local:start
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fullstack_template?schema=public"

# OPTION 2: PostgreSQL with Docker (Local alternative)
# Start with: npm run postgres:docker:start
# DATABASE_URL="postgresql://postgres:postgres123@localhost:5432/fullstack_template?schema=public"

# OPTION 3: SQLite (Only for quick tests)
# DATABASE_URL="file:./dev.db"

# =================================================================
# CLERK AUTHENTICATION (LOCAL DEVELOPMENT)
# =================================================================
# Crear cuenta en: https://dashboard.clerk.com/
# Usar claves de desarrollo por ahora

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_development_key"
CLERK_SECRET_KEY="sk_test_development_key"

# URLs de Clerk (ya configuradas para desarrollo local)
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL="/dashboard"
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL="/dashboard"

# =================================================================
# DEVELOPMENT SETTINGS
# =================================================================
NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Admin user IDs (configurar después de crear usuarios en Clerk)
# ADMIN_USER_IDS="user_xxx,user_yyy"

# =================================================================
# SERVICIOS OPCIONALES (CONFIGURAR CUANDO SEA NECESARIO)
# =================================================================

# File uploads (cuando implementes subida de archivos)
# UPLOADTHING_SECRET="sk_live_xxx"
# UPLOADTHING_APP_ID="xxx"

# Email service (cuando implementes emails)
# RESEND_API_KEY="re_xxx"

# =================================================================
# NOTAS PARA DESARROLLO LOCAL
# =================================================================

# 1. PostgreSQL Local:
#    - Instalar: npm run postgres:local:install
#    - Iniciar:  npm run postgres:local:start
#    - Estado:   npm run postgres:local:status
#    - Crear DB: npm run postgres:local:create-db

# 2. PostgreSQL Docker:
#    - Iniciar:  npm run postgres:docker:start
#    - Estado:   npm run postgres:docker:status
#    - Admin:    http://localhost:8080 (Adminer)
#    - PgAdmin:  http://localhost:5050

# 3. Después de configurar la base de datos:
#    - npm run db:push
#    - npm run db:seed
#    - npm run db:studio

# 4. Para cambiar entre bases de datos:
#    - npm run db:switch:postgresql
#    - npm run db:switch:sqlite